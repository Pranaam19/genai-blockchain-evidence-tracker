{
  "name": "evidence-retention-backend",
  "version": "1.0.0",
  "description": "Blockchain evidence retention system",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network mumbai"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.0.3",
    "ethers": "^6.4.0",
    "hardhat": "^2.22.1"
  }
}
